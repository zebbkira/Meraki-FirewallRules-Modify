# Meraki API 配置
# 从 Meraki Dashboard → Organization → Settings → API access 获取
MERAKI_API_KEY=your_api_key_here

# Flask 监听端口（可选，默认 5000）
FLASK_PORT=5000

# Meraki API 端点（可选，默认中国区）
# 中国区: https://api.meraki.cn/api/v1
# 国际区: https://api.meraki.com/api/v1
MERAKI_BASE_URL=https://api.meraki.cn/api/v1
